<!DOCTYPE html>
<html lang="pl">
    <head>
        <meta charset="UTF-8">
        <title>{{ page.name|e }} &ndash; Website</title>
        <meta name="description" content="Description.">
        <link rel="stylesheet" href="{{ 'page.css'|asset }}">
        <!--[if lt IE 9]>
            <!--<script src="html5shiv.js"></script>
        <![endif]-->
    </head>
    <body>
        <header class="header">
            <h1>Website</h1>
            <nav role="navigation">
                <ul>
                    {% for item in menu %}
                        <li><a href="{{ 'page'|url({'slug': item.slug}) }}"{{ page.id == item.id ? ' class="active"': '' }}>{{ item.name|e }}</a></li>
                    {% endfor %}
                </ul>
            </nav>
        </header>
        <main class="main" role="main">
            {{ page.content }}
        </main>
        <footer class="footer" role="contentinfo">
            <span class="left">Copyright &copy; John Doe, 2015</span>
            <span class="right">Powered by Basalt &bull; <a href="{{ 'dashboard'|url }}">AP</a></span>
        </footer>
    </body>
</html>