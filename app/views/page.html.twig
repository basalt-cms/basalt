<!DOCTYPE html>
<html lang="en">
    <head>
        <title>{{ title|e }}</title>
        <link rel="stylesheet" href="assets/style.css">
    </head>
    <body>
        <nav>
            <ul>
                {% for page in menu %}
                    <li><a href="{{ page.slug }}">{{ page.name|e }}</a></li>
                {% endfor %}
            </ul>
        </nav>
        <h1>{{ page.name|e }}</h1>
        {{ page.content }}
    </body>
</html>