<!DOCTYPE html>
<html>
    <head>
        <title>Test</title>
    </head>
    <body>
        <h1>Test</h1>
        {% for page in pages %}
            <h2>{{ page.name }}</h2>
            <pre>{{ page.content }}</pre>
        {% endfor %}
    </body>
</html>